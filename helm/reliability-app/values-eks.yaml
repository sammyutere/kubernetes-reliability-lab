replicaCount: 3

image:
  repository: REPLACE_WITH_ECR_REPOSITORY_URL
  tag: "0.1.0"
  pullPolicy: IfNotPresent

config:
  APP_ENV: eks-dev

hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 60

pdb:
  enabled: true
  minAvailable: 2

networkPolicy:
  enabled: true
